#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a, b, k;
    cin >> a >> b >> k;
    if(((100 * k) / (a + b)) > 10)
    {
        cout << "Boris, you are punished!";
    }else{
        cout << "You are my sweet baby";
    }
}